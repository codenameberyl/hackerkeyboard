<?xml version="1.0" encoding="utf-8"?>
<!--
    Hosts androidx.emoji2's EmojiPickerView as an alternate IME input view,
    swapped in for the normal keyboard view via LatinIME.showEmojiPicker()/
    hideEmojiPicker() (see the "Emoji" entry in the options menu).
    panel_switcher_row.xml (included below) switches straight to the
    keyboard, clipboard history, or the GIF/sticker picker without going
    back to the keyboard first; its own "emoji" button is disabled here
    since this panel already is Emoji (see LatinIME#getEmojiPickerContainer()).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:orientation="vertical">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

        <include layout="@layout/panel_switcher_row" />

        <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
                android:id="@+id/emoji_picker_backspace"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|end"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                app:icon="@drawable/ic_emoji_backspace"
                app:iconPadding="0dp"
                app:iconGravity="textStart"
                android:contentDescription="@string/emoji_picker_backspace" />

    </LinearLayout>

    <androidx.emoji2.emojipicker.EmojiPickerView
            android:id="@+id/emoji_picker_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/emoji_picker_height" />

</LinearLayout>
