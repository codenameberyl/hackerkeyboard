<?xml version="1.0" encoding="utf-8"?>
<!-- One row in the clipboard history list (clipboard_history_container.xml). Tapping
     the text pastes it and returns to the keyboard; tapping the pin toggles whether
     this entry is exempt from the history size cap and from "Clear", see
     ClipboardHistoryManager#togglePinned() and LatinIME#populateClipboardHistoryList(). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

    <TextView
            android:id="@+id/clipboard_history_item_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:ellipsize="end"
            android:focusable="true"
            android:maxLines="2"
            android:padding="12dp"
            android:textColor="?attr/colorOnSurface"
            android:textSize="16sp" />

    <ImageButton
            android:id="@+id/clipboard_history_item_pin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="12dp"
            android:src="@drawable/ic_pin"
            android:tint="@color/clipboard_pin_tint" />

</LinearLayout>
