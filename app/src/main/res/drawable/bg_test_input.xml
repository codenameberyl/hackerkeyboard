<?xml version="1.0" encoding="utf-8"?>
<!-- Border for the Home screen's "try it out" text field (see main.xml):
     transparent fill, dimmed-yellow border; distinct from the card that
     contains it, per the reference design's nested-border look. Selected
     via a state-list so the border brightens to full-strength yellow while
     focused, same idea as EditText's default focus-highlight behavior. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="10dp" />
            <stroke android:width="1dp" android:color="?attr/colorPrimary" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="10dp" />
            <stroke android:width="1dp" android:color="@color/md_theme_primary_dim" />
        </shape>
    </item>
</selector>
