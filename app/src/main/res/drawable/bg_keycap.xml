<?xml version="1.0" encoding="utf-8"?>
<!-- Outer rounded-square outline for the Home screen header icon (see
     main.xml): transparent fill, yellow border; an "Esc" pill
     (bg_keycap_inner.xml) sits centered inside it, echoing an actual
     keyboard key's raised-cap-within-a-socket look, per the reference
     design. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="16dp" />
    <stroke android:width="1.5dp" android:color="?attr/colorPrimary" />
</shape>
