<?xml version="1.0" encoding="utf-8"?>
<!-- Standard "push_pin" glyph (Material Symbols/Icons, Apache-2.0), used for the
     clipboard history pin toggle; see clipboard_history_item.xml. Tinted via
     @color/clipboard_pin_tint rather than a fixed color here, so the same drawable
     serves both the pinned and unpinned button states. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
        android:width="24dp"
        android:height="24dp"
        android:viewportWidth="24"
        android:viewportHeight="24">
    <path
            android:fillColor="@android:color/white"
            android:pathData="M16,12V4h1V2H7v2h1v8l-2,2v2h5.2v6h1.6v-6H18v-2L16,12z" />
</vector>
