<?xml version="1.0" encoding="utf-8"?>
<!-- Inner "Esc" pill nested inside the header icon's outer outline
     (bg_keycap.xml, see main.xml): transparent fill, dimmed-yellow border. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="3dp" />
    <stroke android:width="1dp" android:color="@color/md_theme_primary_dim" />
</shape>
